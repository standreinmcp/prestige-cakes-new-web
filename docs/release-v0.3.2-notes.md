# Release v0.3.2 — Homepage fixes & admin env refresh

## Homepage

- Hero image sharpness: removed stacked blur on carousel and overlay
- Process section: Figma-aligned icons, card min-height so text is not clipped
- Category carousel: correct Cozonaci photo (cropped from sprite; no nested card screenshot)
- Language selector: RO/EN dropdown with cookie persistence; nav and footer labels translate

## Admin

- `ADMIN_PASSWORD` updated on Vercel (Production, Preview, Development); redeploy to apply
