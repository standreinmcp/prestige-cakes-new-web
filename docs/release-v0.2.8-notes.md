# Release v0.2.8 — Admin authentication

- Password login at `/admin/login`
- Middleware protects `/admin/*` routes
- `ADMIN_PASSWORD` + `ADMIN_SESSION_TOKEN` env vars
- Logout button in admin sidebar
- See `.env.example`
