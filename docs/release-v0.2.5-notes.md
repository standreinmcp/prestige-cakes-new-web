# Release v0.2.5 — Convex storefront + product cards

- Catalog pages read from Convex (`listLive`, `listMadeToOrder`) with static fallback
- PDP via `getBySlug` with static fallback
- Product card quick-add with „Produs adăugat în coș” feedback
- Gallery sync from product `imageUrl`
- `seedStorefront:importStorefrontCatalog` mutation
- Tests: `catalog-mapper.test.ts`
